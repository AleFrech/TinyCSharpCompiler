public class Program
    {
        public static void Main(string[] args)
        {
            var sum = new Sum();
            var x= 70;
            System.Console.WriteLine(x+sum.Operate(5,25));

            // Animal animal = new Cat();
            // animal.Talk();

            // animal = new Dog();
            // animal.Talk();
        }
    }